# CHANGELOG

本项目所有值得记录的版本变更。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **机器人接口与 skill 支持按昵称**：`POST/GET /api/public/characters` 的 `account`/`nickname` 恰好二选一（昵称全局唯一，可无歧义定位用户），响应回填解析出的账号与昵称；AstrBot skill 默认昵称优先、404 回退账号，支持 `--by-nickname` / `--by-account` 强制模式
- **机器人自助注册**：`POST /api/public/register` 按 QQ 号免码注册（账号=密码=昵称=QQ号，纯数字 6-64 位）；AstrBot skill 支持「QQ号注册 / 注册 QQ号」消息
- **机器人攻坚信息查询**：`GET /api/public/raids/{id}` 返回完整攻坚详情；AstrBot skill「dnfer-raids」支持「打团信息 / 排表信息 / 第 n 波 / 前 n 波 / 查找某时间（周X/上午下午晚上）的团」，按时间匹配或取离当前最近的一场，回复按小队分组的昵称·角色·职责
- **攻坚报名**：列表可报名/取消报名；团长（发起人）默认参加；管理员排表仅可选已报名用户的角色；详情页展示已报名名单并支持取消（自动撤下占位）；未报名用户占位时引导先报名

## [v1.5] - 2026-09-21

昵称与头像：账号昵称全局唯一，支持 S3 头像上传与展示。

### 新增

- **昵称**：注册 / 修改昵称强制全局唯一，仅允许中文、字母、数字
- **头像**：上传至 S3 公开读桶，顶栏与排表占位格展示归属者头像；个人信息面板支持改昵称 / 换头像

### 工程

- 上线前置：需在 `.env` 配置 `DNFER_S3_*`（S3 公开读桶相关参数），头像上传功能方可使用

## [v1.4] - 2026-09-20

管理员自由调整站位。

### 新增

- **管理员站位调整**：管理员可往任意空格放入任意玩家角色；对已占位格直接换人（自动撤下原角色）；点已占位格弹操作菜单（换人 / 拿起移动 / 撤下）
- **移动/对调**：管理员「拿起」角色后点击目标格，空格=搬移、占位格=两人对调（支持跨波次），自动校验小队组成与同波同玩家限制，违规则回滚
- **全玩家选角**：管理员选人弹窗新增玩家选择器（可搜索昵称/角色名），浏览并选用任意玩家的角色（过滤无角色玩家）
- **权限修正**：非管理员「自己的格子」判定改为按角色归属（owner_id），管理员代为调整后，角色本人仍可正常撤下/改职责/删波次

### 变更

- 排表页移动态下格子高亮可落目标，源格标记「移动中」；点源格 / Esc / 取消按钮退出移动态

## [v1.3] - 2026-09-20

首个正式记录版本：攻坚排表工具当前全部功能（DNF 游戏群团队管理 + 攻坚排表，多端实时同步，含群机器人 API）。

### 新增

- **认证与管理员体系**：首次启动自动创建管理员；管理员生成注册码，新用户凭码注册（支持一次性 / 多用户 / 有效期）；JWT 登录
- **成员与角色管理**：成员群昵称 + 多个角色 CRUD（所有权校验）；输出角色记录模拟伤害、秒伤，辅助角色记录增益量
- **职业体系**：16 大类 × 子职业的职业树（`职业信息.json` 已入库）；`输出/辅助` 由 5 个辅助职业自动推导；角色管理、攻坚排表、选角弹窗展示职业图标，缺失自动回退占位图
- **攻坚排表**：管理员发起攻坚（规模 4/8/12/16/20，默认 12）；波次表格按小队明亮配色区分；成员点空格选自己的角色占位；职责下拉按职业过滤（输出→主C/辅C/划水，辅助→主奶/太阳奶/划水）；校验规则（角色全攻坚唯一、每小队至多 1 名主奶、满员小队硬校验、同波冲突自动替换）
- **副本预设**：管理端维护副本预设，发起攻坚时选择副本并带入规模，发起时间必填；攻坚详情展示副本与发起时间
- **权限控制**：管理员锁定/解锁、编辑任意格子、加波/删波、删除攻坚（级联清理波次与格子）
- **实时同步**：WebSocket 增量广播，断线自动重连；客户端乐观更新、按版本号合并，冲突以服务器为准
- **机器人 API**：固定 Token 鉴权；查询攻坚列表与单波配置、按账号批量录入/查询角色
- **AstrBot skill**：dnfer 角色录入/查询技能包
- **DNF 鎏金风 UI**：引入 Naive UI 定制鎏金主题；登录/注册、攻坚列表、排表详情、管理页、我的角色页统一面板卡片化
- **通知封装**：notify 模块（discrete dialog/message）统一替换全站原生 alert/confirm/notice
- **排表小队统计**：排表表头下显示各小队输出职业总模拟/秒伤求和（纯前端计算）；数值全 0 未录入时隐藏
- **角色表单组件化**：角色创建/编辑表单抽出为 CharacterForm 组件；创建表单渲染于表头下方，编辑表单内联于对应卡片下方
- **部署**：Docker Compose（应用容器 + nginx 反向代理 + TLS），静态前端由后端托管，SPA 深链兜底；幂等迁移保证存量库升级不崩溃

### 变更

- 顶导航 Logo 文案改为「跨六 DNF 糕手」
- 角色数值展示统一为亿单位，表单改为标签化
- 排表波次改 CSS Grid 按小队数动态列，格子改小队 tint 卡片
- 职责下拉改 NSelect；攻坚创建表单改 Naive 组件化

### 修复

- 攻坚创建表单副本选择改 `v-model:value`，消除显示与状态脱节风险
- 非管理员不再请求副本列表，避免列表页 403
- 存量库启动时自动运行幂等迁移（副本、职业、遗留 NOT NULL 列清理），避免升级崩溃
- notify.confirmDialog 补遮罩点击/ESC 关闭兜底，避免 Promise 挂起
- 职业树加载竞态与未选职业提示
- 角色保存错误透出与切换职业清空失效统计
- 登录 401 不再误跳转，错误信息透出
- 单次验证码（single-use）与 JWT sub 解析防护
- 清理无引用的死 CSS 选择器

### 工程

- 前端 Vue 3 + TypeScript + Vite + Pinia + Vue Router + Naive UI；后端 FastAPI + SQLAlchemy 2 + SQLite
- 后端 pytest 与前端 Vitest 全量测试覆盖（含边界用例）
- 职业图标经 Docker 卷挂载不入库，补图即自动显示；`.gitignore` 忽略 `.DS_Store`
